# Copie para .env quando precisar. Em dev com proxy (npm run dev) pode deixar vazio.

# URL base da API. Vazio = mesmo origem (recomendado quando o front é servido pelo backend em produção).
# Use quando front e API estão em hosts diferentes (ex.: https://api.seudominio.com).
VITE_API_BASE_URL=
