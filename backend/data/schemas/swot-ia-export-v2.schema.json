{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://aegis.valorian.local/schemas/swot-ia-export-v2.json",
  "title": "AEGIS SWOT de IA — export/import v3",
  "description": "Documento portátil da SWOT de IA. Pilares por quadrante partem do banco de itens; pilares extras (canônicos ou custom) são declarados em payload.pilares. Compatível com o método Valorian.",
  "type": "object",
  "required": ["format", "version", "payload"],
  "additionalProperties": false,
  "properties": {
    "format": {
      "const": "aegis.swot-ia",
      "description": "Identificador estável do formato."
    },
    "version": {
      "type": "integer",
      "enum": [2, 3],
      "description": "v3 declara pilares por quadrante; v2 continua aceito na importação (defaults do banco)."
    },
    "exported_at": {
      "type": "string",
      "format": "date-time"
    },
    "locale": {
      "type": "string",
      "default": "pt-BR"
    },
    "meta": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "title": { "type": "string", "maxLength": 300 },
        "organization": { "type": "string", "maxLength": 200 },
        "source": {
          "type": "string",
          "enum": ["platform", "manual", "import", "example", "user", "draft"]
        },
        "notes": { "type": "string", "maxLength": 2000 }
      }
    },
    "payload": {
      "$ref": "#/$defs/SwotPayloadV3"
    }
  },
  "$defs": {
    "PilarId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]{0,39}$",
      "description": "Id do pilar: canônico (dados, talento, …) ou custom criado pelo usuário."
    },
    "PilarSlot": {
      "type": "object",
      "required": ["id"],
      "additionalProperties": false,
      "properties": {
        "id": { "$ref": "#/$defs/PilarId" },
        "nome": {
          "type": "string",
          "maxLength": 80,
          "description": "Rótulo de exibição no quadrante (ex.: label do banco de itens)."
        }
      }
    },
    "PilaresPorQuadrante": {
      "type": "object",
      "additionalProperties": false,
      "description": "Slots de pilar ativos em cada quadrante (ordem = ordem de exibição). Defaults = banco de itens.",
      "properties": {
        "forcas": {
          "type": "array",
          "minItems": 1,
          "maxItems": 12,
          "items": { "$ref": "#/$defs/PilarSlot" }
        },
        "fraquezas": {
          "type": "array",
          "minItems": 1,
          "maxItems": 12,
          "items": { "$ref": "#/$defs/PilarSlot" }
        },
        "oportunidades": {
          "type": "array",
          "minItems": 1,
          "maxItems": 12,
          "items": { "$ref": "#/$defs/PilarSlot" }
        },
        "ameacas": {
          "type": "array",
          "minItems": 1,
          "maxItems": 12,
          "items": { "$ref": "#/$defs/PilarSlot" }
        }
      }
    },
    "Score": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5,
      "description": "Escala 1–5 (1 = baixo, 5 = alto)."
    },
    "ItemInterno": {
      "type": "object",
      "required": ["id", "texto", "pilar"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "maxLength": 64,
          "description": "Identificador estável no documento (ex.: f1, fx2). Usado nos cruzamentos TOWS."
        },
        "texto": { "type": "string", "minLength": 1, "maxLength": 500 },
        "pilar": { "$ref": "#/$defs/PilarId" },
        "impacto": { "$ref": "#/$defs/Score" },
        "viabilidade": {
          "allOf": [{ "$ref": "#/$defs/Score" }],
          "description": "Para Forças: viabilidade de explorar; para Fraquezas: viabilidade de corrigir."
        },
        "evidencia": {
          "type": "string",
          "maxLength": 1000,
          "description": "Fato ou métrica que ancora o item (regra de evidência)."
        },
        "prioridade": {
          "type": "integer",
          "minimum": 1,
          "maximum": 40,
          "description": "Ordem explícita após priorização (1 = mais prioritário)."
        }
      }
    },
    "ItemExterno": {
      "type": "object",
      "required": ["id", "texto", "pilar"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "maxLength": 64
        },
        "texto": { "type": "string", "minLength": 1, "maxLength": 500 },
        "pilar": { "$ref": "#/$defs/PilarId" },
        "impacto": { "$ref": "#/$defs/Score" },
        "probabilidade": {
          "allOf": [{ "$ref": "#/$defs/Score" }],
          "description": "Probabilidade de a oportunidade/ameaça se materializar no horizonte da estratégia."
        },
        "evidencia": { "type": "string", "maxLength": 1000 },
        "prioridade": {
          "type": "integer",
          "minimum": 1,
          "maximum": 40
        }
      }
    },
    "IniciativaTows": {
      "type": "object",
      "required": ["acao"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "maxLength": 64 },
        "acao": { "type": "string", "minLength": 1, "maxLength": 1000 },
        "dono": { "type": "string", "maxLength": 200 },
        "horizonte": { "type": "string", "maxLength": 120 },
        "itens_internos": {
          "type": "array",
          "maxItems": 10,
          "items": { "type": "string" },
          "description": "IDs de forcas ou fraquezas cruzados nesta iniciativa."
        },
        "itens_externos": {
          "type": "array",
          "maxItems": 10,
          "items": { "type": "string" },
          "description": "IDs de oportunidades ou ameacas cruzados nesta iniciativa."
        }
      }
    },
    "VereditoTipo": {
      "type": "string",
      "enum": ["sustenta", "executavel", "fundacao", "repensar", ""]
    },
    "SwotPayloadV3": {
      "type": "object",
      "required": [
        "optica",
        "forcas",
        "fraquezas",
        "oportunidades",
        "ameacas",
        "tows_fo",
        "tows_fa",
        "tows_fxo",
        "tows_fxa",
        "veredito_tipo",
        "veredito_titulo",
        "veredito_texto"
      ],
      "additionalProperties": false,
      "properties": {
        "optica": {
          "type": "string",
          "maxLength": 2000,
          "description": "Estratégia organizacional de IA — a lente da análise."
        },
        "pilares": {
          "$ref": "#/$defs/PilaresPorQuadrante"
        },
        "forcas": {
          "type": "array",
          "maxItems": 40,
          "items": { "$ref": "#/$defs/ItemInterno" }
        },
        "fraquezas": {
          "type": "array",
          "maxItems": 40,
          "items": { "$ref": "#/$defs/ItemInterno" }
        },
        "oportunidades": {
          "type": "array",
          "maxItems": 40,
          "items": { "$ref": "#/$defs/ItemExterno" }
        },
        "ameacas": {
          "type": "array",
          "maxItems": 40,
          "items": { "$ref": "#/$defs/ItemExterno" }
        },
        "tows_fo": {
          "type": "array",
          "maxItems": 20,
          "description": "F × O · Ofensiva",
          "items": { "$ref": "#/$defs/IniciativaTows" }
        },
        "tows_fa": {
          "type": "array",
          "maxItems": 20,
          "description": "F × A · Defesa",
          "items": { "$ref": "#/$defs/IniciativaTows" }
        },
        "tows_fxo": {
          "type": "array",
          "maxItems": 20,
          "description": "f × O · Reforço",
          "items": { "$ref": "#/$defs/IniciativaTows" }
        },
        "tows_fxa": {
          "type": "array",
          "maxItems": 20,
          "description": "f × A · Sobrevivência",
          "items": { "$ref": "#/$defs/IniciativaTows" }
        },
        "veredito_tipo": { "$ref": "#/$defs/VereditoTipo" },
        "veredito_titulo": { "type": "string", "maxLength": 300 },
        "veredito_texto": { "type": "string", "maxLength": 8000 }
      }
    }
  }
}
